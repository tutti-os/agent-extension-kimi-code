# Reference review

The runtime package, CLI entry point, and ACP subcommand are owned by the
upstream `MoonshotAI/kimi-cli` project. Tutti's package carries no upstream
executable and installs the exact PyPI release only after confirmation.
