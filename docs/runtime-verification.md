# Runtime verification

The extension contract is pinned to `kimi-cli==1.49.0` and launches `kimi acp`.
Compatibility is bounded to `>=1.49.0 <2.0.0`; discovery must complete a
standard ACP initialize probe within 15 seconds.
