# Kimi Code package

The runtime provider is `acp:kimi-code` and the signing key ID is `tutti-kimi-code-release-v1`. Keep all package content declarative.

`icon.png` is the colored shared branded identity used in the Provider Rail,
conversation headers, Message Center, and mentions. `mask-icon.png` is the
transparent conversation-row mask glyph.
