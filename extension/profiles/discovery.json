{
  "schemaVersion": "tutti.agent.discovery.v1",
  "candidates": [{
    "binaryNames": ["kimi"],
    "version": { "args": ["--version"], "constraint": ">=1.49.0 <2.0.0" },
    "launchArgs": ["acp"],
    "probe": { "kind": "acp-initialize", "timeoutMs": 15000 }
  }]
}
